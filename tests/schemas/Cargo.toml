[package]
name = "schemas"
version = "0.1.0"
edition = "2024"

[dependencies]
schemas_subcrate = { path = "schemas_subcrate" }

rawr = { workspace = true }
serde = { workspace = true, features = ["derive"] }
